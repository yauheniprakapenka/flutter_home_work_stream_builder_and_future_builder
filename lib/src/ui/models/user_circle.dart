import 'package:flutter/material.dart';

class UserCircle {
  final Offset offset;
  final double radius;
  final Color color;

  const UserCircle({
    required this.offset,
    required this.radius,
    required this.color,
  });
}
